# Система управления фильмами и сериалами

Веб-приложение для создания и управления личной коллекцией фильмов и сериалов.

## 📁 Структура проекта

```
project/
├── index1.html              # Главная страница
├── index2.html              # Страница категорий
├── edit-film.html           # Страница редактирования фильма
├── server.js                # Backend сервер (Node.js + Express)
├── package.json             # Зависимости Node.js
├── ALL_CATEGORIES/          # Папка с глобальными категориями
│   └── categories.json      # Список всех категорий
├── films_and_serials/       # Папка с данными фильмов (создается автоматически)
│   ├── 1/
│   │   ├── index.html       # Страница фильма
│   │   ├── data.json        # Все данные (название, рейтинг, описание, отзыв)
│   │   ├── banner/          # Главный постер (16:9)
│   │   │   └── banner.ext
│   │   └── preview/         # Превью изображения (16:9)
│   │       ├── preview1/
│   │       │   └── preview.ext
│   │       ├── preview2/
│   │       │   └── preview.ext
│   │       └── preview3/
│   │           └── preview.ext
│   ├── 2/
│   └── ...
```

## 🚀 Установка и запуск

### ⚡ БЫСТРЫЙ ЗАПУСК (Windows)

**Просто запустите файл `ЗАПУСК.bat`** - всё остальное сделается автоматически!

Или используйте отдельные скрипты:

#### Все BAT скрипты:

1. **`ЗАПУСК.bat`** - Главное меню (рекомендуется)
   - Интерактивное меню для всех операций
   - Проверка зависимостей
   - Запуск сервера
   - Открытие в браузере

2. **`start-server.bat`** - Быстрый запуск сервера
   - Автоматическая проверка Node.js
   - Автоматическая установка зависимостей
   - Запуск сервера на порту 3000

3. **`start-server-dev.bat`** - Запуск в режиме разработки
   - Автоперезагрузка при изменении файлов
   - Использует nodemon

4. **`install-dependencies.bat`** - Установка/обновление зависимостей
   - Проверка Node.js
   - Чистая установка пакетов
   - Показ установленных пакетов

5. **`open-in-browser.bat`** - Открытие в браузере
   - Проверка работы сервера
   - Автозапуск сервера если не запущен
   - Открытие страницы

6. **`migrate-old-data.bat`** - Миграция старых данных
   - Удаляет устаревшие папки description/ и personal_description/
   - Все данные теперь хранятся в data.json

---

### 📝 Ручная установка (для продвинутых)

#### Шаг 1: Установка Node.js
1. Скачайте и установите Node.js: https://nodejs.org/
2. Проверьте установку:
```bash
node --version
npm --version
```

#### Шаг 2: Установка зависимостей
```bash
npm install
```

#### Шаг 3: Запуск сервера
```bash
npm start
```

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

#### Шаг 4: Открытие приложения
Откройте в браузере: `http://localhost:3000/index1.html`

## 📝 Использование

### Создание новой карточки фильма:
1. Наведите мышку на нижнюю часть экрана
2. Нажмите на кнопку "+" когда она появится
3. Нажмите на карандаш ✏️ для входа в режим редактирования
4. Заполните информацию о фильме:
   - Название
   - Загрузите главный постер (16:9)
   - Загрузите 3 превью изображения
   - Поставьте рейтинг по 10-бальной шкале (половинки и полные звёзды)
   - Напишите описание фильма
   - Заполните метаданные (год, страна, режиссёр и т.д.)
   - Напишите личный отзыв
5. Нажмите "Сохранить"

### Особенности рейтинга:
- **10-бальная система**: от 0 до 10 (цена деления 1)
- **5 звёзд = 10 баллов**: каждая звезда состоит из двух половинок
- **Половина звезды = 1 балл**: кликайте на левую половину (1, 3, 5, 7, 9)
- **Полная звезда = 2 балла**: кликайте на правую половину (2, 4, 6, 8, 10)
- **Подсказка**: при наведении показывает точный рейтинг (например, "3" или "7")
- **Отображение**: крупное число рейтинга "/10" справа от звёзд

### Режимы работы:
- **Режим просмотра** (по умолчанию): можно только смотреть данные
- **Режим редактирования**: нажмите на карандаш ✏️ для редактирования
- После сохранения автоматически выходит из режима редактирования

### Структура данных (data.json):
```json
{
  "title": "Название фильма",
  "rating": 8,
  "year": "2024",
  "country": "США",
  "director": "Режиссёр",
  "cast": "Актёры",
  "mainCategory": "Основная категория",
  "subCategory": "Подкатегория",
  "subCategory2": "Подкатегория 2",
  "subCategory3": "Подкатегория 3",
  "filmDescription": "Описание фильма...",
  "personalReview": "Мой отзыв...",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z"
}
```

## 🎨 Цветовая схема

- Фон: `#141414`
- Карточки: `#212121`
- Элементы: `#3e3e3e`
- Hover: `#323232`
- Текст: `#9E9E9E`
- Звёзды: `#FFD700`

## 🔧 API Endpoints

### Получить все фильмы
```
GET /api/films
```

### Получить конкретный фильм
```
GET /api/films/:id
```

### Создать новый фильм
```
POST /api/films
Content-Type: application/json

{
  "title": "Название",
  "stars": 5
}
```

### Обновить фильм
```
PUT /api/films/:id
Content-Type: application/json

{
  "title": "Новое название"
}
```

### Загрузить файл (баннер/превью)
```
POST /api/films/:id/upload/:type
Content-Type: multipart/form-data

file: [binary]
```
Где `type` может быть: `banner`, `preview`

### Сохранить текстовое описание
```
POST /api/films/:id/description/:type
Content-Type: application/json

{
  "content": "Текст описания"
}
```
Где `type` может быть: `description`, `personal_description`

### Удалить фильм
```
DELETE /api/films/:id
```

## 📦 Зависимости

- **express** - веб-фреймворк
- **multer** - загрузка файлов
- **cors** - CORS поддержка
- **nodemon** - автоперезагрузка сервера (dev)

## 🔮 Будущие возможности

- [ ] Интеграция с TMDB API для автозаполнения данных
- [ ] Интеграция с Kinopoisk API
- [ ] Поиск и фильтрация фильмов
- [ ] Экспорт/импорт данных
- [ ] Резервное копирование
- [ ] Статистика просмотров
- [ ] Рекомендации на основе рейтингов

## 🐛 Решение проблем

### Порт 3000 занят
Измените PORT в `server.js`:
```javascript
const PORT = 3001; // или другой свободный порт
```

### Ошибка при загрузке файлов
Убедитесь, что у сервера есть права на запись в папку `films_and_serials`

### Сервер не запускается
1. Удалите `node_modules` и `package-lock.json`
2. Выполните `npm install` заново
3. Проверьте версию Node.js (рекомендуется 14+)

## 📄 Лицензия

ISC

